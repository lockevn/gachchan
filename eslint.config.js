// for eslint . --fix

// use type=module in package.json if we want to use ES6 modules `export default {}`

export default {
  ignores: [
    'node_modules',
    '.parcel-cache',
    'dist',
    '.eslintrc.js',
    'package.json',
    'package-lock.json',
    'coverage',
    //
  ],
}
